name: Archive content

on:
  schedule:
    - cron: '30 0 * * *'
  workflow_dispatch:
    
jobs:
  archive:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Run the archive process
      run: docker run -v ${PWD}:/app ghcr.io/piraces/devsectricks:main
